import { TextRunningDirective } from './text-running.directive';

describe('TextRunningDirective', () => {
  it('should create an instance', () => {
    const directive = new TextRunningDirective();
    expect(directive).toBeTruthy();
  });
});
